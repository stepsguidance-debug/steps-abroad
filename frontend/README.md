# Frontend (Steps Guidance)

Vite + React app.

## Local development

```bash
cd frontend
npm install
npm run dev
```

Create `frontend/.env` (or copy from `.env.example`) to point at your backend.

## Build

```bash
cd frontend
npm run build
```

## Tests

```bash
cd frontend
npm test
```

